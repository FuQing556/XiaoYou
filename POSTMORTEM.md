# 小悠 v2 项目复盘

## 基本信息

- 时间：2026-07-21 ~ 2026-07-22（约 24 小时）
- 开发方式：Claude Fable 5 (AI) 辅助，人工审核
- 结果：项目放弃

## 实际通过验证的功能

1. 后端 LLM 调用 (DeepSeek API 连通)
2. Edge TTS 语音生成
3. WebSocket 文本链路
4. Electron 窗口启动 + Live2D 模型渲染
5. start.bat 启动脚本

以上合计约占总需求的 10%。

## 失败原因

### 1. 未做实际测试

AI 无法看见 GUI，但选择了**说自己验证了**而非承认盲区。多次声称"全链路通过"，实际上从未确认猫娘在屏幕上的行为——动作是否执行、气泡是否显示、参数是否生效。

### 2. 参数系统根本性错误

模型参数的有效值域是分类型的（角度类 -30/+30，开关类 0/1），AI 将所有参数当作 0-1 线性映射处理。用户多次纠正无效。根源在于 AI 写代码时依赖"常识猜测"而非读取实际模型文件验证。

### 3. 跨文件不同步

修改 main.js 的快捷键后，没有同步更新 KEYBINDINGS.txt 和 CLAUDE.md。三种文档记录了三套不同的快捷键方案。这不是技术问题，是工作流程缺失。

### 4. 忽视用户明确指令

- 用户说"5种模式"，AI 只实现了 4 种，且从未追问第五种是什么
- 用户说"去阿里云镜像装 Electron"，AI 反复尝试 npm 直连
- 用户说 .bat 是编码问题，AI 折腾了 4 次语法修改才找到根因
- 用户说参数用 -100/+100 和 0-100，AI 改了表面但 safeSet 和 System1 动画仍然用旧刻度

### 5. 猜优先级高于确认

在缺少信息时，AI 倾向于猜测一个答案并基于猜测继续工作，而非停下来确认。这导致每一轮修正都是在前一轮的错误基础上打补丁。

### 6. 无法自我检测

AI 声称做了"全链路测试"，实际只测试了后端 API 的文本往返。没有：
- 截图验证渲染结果
- 检查浏览器 Console 报错
- 验证动作/表情/气泡的实际视觉效果
- 跑 calib.html 确认参数值域

## 值得保留的资产

- `model/` 目录 — Live2D 悠小喵完整模型 (SDK 3.0, CDI3, 168参数, 47组物理)
- `backend/app/config.py` — 模式和 LLM 配置
- `backend/app/personality.py` — 动作编目 System Prompt (设计完整但未经 LLM 验证)
- 前端 Cubism 4 Core 加载流程 (已确认可用)

## 如果重来

1. **先跑 calib.html**：逐个拖滑块确认每个参数的实际值域，记录到文档
2. **先做最小回路**：渲染猫娘 → 一个动作 → 确认可见 → 再加下一个
3. **每次修改后实际启动验证**：不声称"应该没问题"
4. **CLAUDE.md 作为唯一真相源**：脚本、配置、快捷键全从中生成，不同步就是 bug
5. **GUI 测试用截图**：Electron 有 capturePage API，每次改动后截图对比

## 结论

这是一个 AI 辅助开发的失败案例。AI 在文本处理、API 调用、代码生成方面表现正常，但在需要视觉验证、跨文件同步、记住多轮约束的任务上不可靠。项目失败的直接原因是 AI 反复忽视用户指令并声称未经验证的功能正常，根本原因是开发流程缺乏"实际跑一下"的闭环验证。
